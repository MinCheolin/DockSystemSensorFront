<Window x:Class="ShipyardDashboard.Views.EquipmentDetailView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ShipyardDashboard.ViewModels"
        xmlns:oxy="http://oxyplot.org/wpf"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:EquipmentDetailViewModel}"
        Title="장비 상세 정보" Height="700" Width="800"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource LightThemeBackgroundBrush}">
    <Window.Resources>
        <Style x:Key="HealthStatusIndicatorStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Text" Value="🟢 정상"/>
            <Setter Property="Foreground" Value="{StaticResource LightThemeSuccessBrush}"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Status}" Value="주의">
                    <Setter Property="Text" Value="🟡 주의"/>
                    <Setter Property="Foreground" Value="{StaticResource StatusWarningBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="위험">
                    <Setter Property="Text" Value="🔴 위험"/>
                    <Setter Property="Foreground" Value="{StaticResource StatusErrorBrush}"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding EquipmentGroup.GroupName}" Style="{StaticResource TitleTextBlockStyle}" Margin="15"/>

        <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}" Margin="15">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding IndividualEquipments}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <VirtualizingStackPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="{StaticResource LightThemeBorderBrush}" BorderThickness="0,0,0,1" Padding="15" Margin="0,2">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- Row 0: Basic Info -->
                                    <StackPanel Grid.Row="0">
                                        <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16" Foreground="{StaticResource LightThemePrimaryTextBrush}"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                            <TextBlock Text="상태: " Foreground="{StaticResource LightThemeSecondaryTextBrush}"/>
                                            <TextBlock Text="{Binding Status}" FontWeight="SemiBold" Foreground="{StaticResource LightThemeAccentBrush}"/>
                                        </StackPanel>
                                        <ItemsControl ItemsSource="{Binding Details}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal" Margin="10,2,0,0">
                                                        <TextBlock Foreground="{StaticResource LightThemeSecondaryTextBrush}">
                                                            <Run Text="{Binding Key, Mode=OneWay}"/>
                                                            <Run Text=": "/>
                                                        </TextBlock>
                                                        <TextBlock Text="{Binding Value.Display}" Foreground="{StaticResource LightThemePrimaryTextBrush}"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>

                                    <!-- Row 1: Health Status & Predictive Maintenance -->
                                    <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}" Margin="0,15,0,5" Padding="10">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,20,0">
                                                <TextBlock Text="건강 상태" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,0,0,5"/>
                                                <TextBlock Style="{StaticResource HealthStatusIndicatorStyle}" DataContext="{Binding Health}"/>
                                            </StackPanel>

                                            <Grid Grid.Column="1" Margin="10,0">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="*"/>
                                                </Grid.RowDefinitions>
                                                <TextBlock Text="실시간 진동 (g)" FontSize="12" Foreground="{StaticResource LightThemeSecondaryTextBrush}"/>
                                                <oxy:PlotView Grid.Row="1" Model="{Binding Health.VibrationPlotModel}" Height="50"/>
                                            </Grid>

                                            <Grid Grid.Column="2" Margin="10,0">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="*"/>
                                                </Grid.RowDefinitions>
                                                <TextBlock Text="전류 소모량 (A)" FontSize="12" Foreground="{StaticResource LightThemeSecondaryTextBrush}"/>
                                                <oxy:PlotView Grid.Row="1" Model="{Binding Health.CurrentPlotModel}" Height="50"/>
                                            </Grid>
                                        </Grid>
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <Button Grid.Row="2" Content="닫기" Command="{Binding CloseCommand}" CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}" 
                Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Right" Margin="15" Width="100"/>
    </Grid>
</Window>